version: "3.8"
services:
  bot:
    build: .
    restart: always
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_ID=${ADMIN_ID}
      - RENDER_EXTERNAL_URL=${RENDER_EXTERNAL_URL}
      - PORT=10000
    ports:
      - "10000:10000"
    volumes:
      - .:/app
